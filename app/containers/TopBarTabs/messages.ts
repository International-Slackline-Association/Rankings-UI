/*
 * TopBarTabs Messages
 *
 * This contains all the text for the TopBarTabs component.
 */

import { defineMessages } from 'react-intl';
const scope = 'app.containers.TopBarTabs';

export default defineMessages({
  rankings: {
    id: `${scope}.rankings`,
    defaultMessage: 'Rankings',
  },
  contests: {
    id: `${scope}.contests`,
    defaultMessage: 'Contests',
  },
});
