import { defineMessages } from 'react-intl';
const scope = 'app.containers.TableSearchInput';

export default defineMessages({
  rankings: {
    id: `${scope}.placeholder`,
    defaultMessage: 'Search Athlete',
  },
});
