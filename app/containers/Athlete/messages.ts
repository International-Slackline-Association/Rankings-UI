/*
 * Rankings Messages
 *
 * This contains all the text for the Rankings component.
 */

import { defineMessages } from 'react-intl';
const scope = 'app.containers.something';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Rankings component !',
  },
});
