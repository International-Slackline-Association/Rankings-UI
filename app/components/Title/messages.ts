import { defineMessages } from 'react-intl';

const scope = 'app.components.Title';

export default defineMessages({
  titleAltText: {
    id: `${scope}.titleAltText`,
    defaultMessage: 'SLACKLINE WORLD RANKING LIST',
  },
});
