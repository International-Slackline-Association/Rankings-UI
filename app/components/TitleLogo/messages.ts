/*
 * TitleLogo Messages
 *
 * This contains all the text for the TitleLogo component.
 */

import { defineMessages } from 'react-intl';

const scope = 'app.components.Title';

export default defineMessages({
  titleLogoAltText: {
    id: `${scope}.titleLogoAltText`,
    defaultMessage: 'Logo',
  },
});
