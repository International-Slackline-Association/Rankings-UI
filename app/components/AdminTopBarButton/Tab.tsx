import { Tab } from '@material-ui/core';
import styled from 'styles/styled-components';
import * as React from 'react';

const tab = props => <Tab {...props} />;

const CustomTab = styled(tab)``;

export default CustomTab;
