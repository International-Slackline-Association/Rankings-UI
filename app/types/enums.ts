export enum TopBarTabType {
  Static = 'Static',
  Dynamic = 'Dynamic',
}

export enum TopBarTabContentType {
  rankings = 'rankings',
  contests = 'contests',
  athlete = 'athelete',
  contest = 'contest',
}
